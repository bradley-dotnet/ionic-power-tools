import { DynamicRouterLinkDirective } from './dynamic-router-link.directive';

describe('RouterLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicRouterLinkDirective();
    expect(directive).toBeTruthy();
  });
});
